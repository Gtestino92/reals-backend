## Repository Agent Rules

- Do not create commits unless the user explicitly asks for a commit after they have reviewed or validated the changes.
- Do not push to any remote branch unless the user explicitly asks for that exact push.
- Never push directly to `development` unless the user explicitly asks to push to `development`.
