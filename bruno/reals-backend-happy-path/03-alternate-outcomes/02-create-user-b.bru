meta {
  name: 02 Create User B
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/users
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{alternateUserBEmail}}"
  }
}

script:post-response {
  bru.setEnvVar("alternateUserBId", res.getBody().id);
}
