meta {
  name: 36 User A Add Proposal
  type: http
  seq: 36
}

post {
  url: {{baseUrl}}/api/connections/{{negativeConnectionId}}/proposals
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  X-Dev-User-Id: {{negativeUserAId}}
}

body:json {
  {
    "proposedDateTime": "{{negativeSecondChatDateTime}}"
  }
}

script:post-response {
  bru.setEnvVar("negativeProposalAId", res.getBody().id);
}
