meta {
  name: 40 User B Add Different Proposal
  type: http
  seq: 40
}

post {
  url: {{baseUrl}}/api/connections/{{negativeConnectionId}}/proposals
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  X-Dev-User-Id: {{negativeUserBId}}
}

body:json {
  {
    "proposedDateTime": "{{negativeOtherSecondChatDateTime}}"
  }
}

script:post-response {
  bru.setEnvVar("negativeProposalBId", res.getBody().id);
}
