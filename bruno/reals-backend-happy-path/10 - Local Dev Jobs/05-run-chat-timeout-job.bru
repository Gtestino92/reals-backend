meta {
  name: 05 Run Chat Timeout Job
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/local-dev/jobs/chat-timeout/run
  body: none
  auth: none
}

script:post-response {
  if (res.getBody().job !== "ChatTimeoutJob") {
    throw new Error("Expected ChatTimeoutJob to run.");
  }
}
