meta {
  name: 10 - Local Dev Jobs
  seq: 10
}

docs {
  Local-only manual triggers for periodic backend jobs. These endpoints are available only in local profiles and should be used to test scheduler behavior deterministically while scheduler.enabled=false.

  Dev/prod run these through Spring scheduling and ShedLock instead.
}
