meta {
  name: Security
  type: folder
  seq: 3
}

docs {
  Local security checks.

  Rate limit smoke test precondition:
  start the backend with RATE_LIMIT_DEFAULT_CAPACITY=1,
  RATE_LIMIT_DEFAULT_REFILL_TOKENS=1 and RATE_LIMIT_DEFAULT_REFILL_PERIOD_SECONDS=60.
}
