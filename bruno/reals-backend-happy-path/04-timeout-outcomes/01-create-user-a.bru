meta {
  name: 01 Create User A
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/users
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{timeoutUserAEmail}}"
  }
}

script:post-response {
  bru.setEnvVar("timeoutUserAId", res.getBody().id);
}
