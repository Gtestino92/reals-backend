meta {
  name: 56 Run Chat Timeout Job For Second Chat
  type: http
  seq: 56
}

post {
  url: {{baseUrl}}/api/dev/jobs/chat-timeout/run
  body: none
  auth: none
}

script:post-response {
  if (res.getBody().job !== "ChatTimeoutJob") {
    throw new Error("Expected ChatTimeoutJob to run.");
  }
}
