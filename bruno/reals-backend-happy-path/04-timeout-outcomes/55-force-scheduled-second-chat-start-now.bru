meta {
  name: 55 Force Scheduled Second Chat Start Now
  type: http
  seq: 55
}

post {
  url: {{baseUrl}}/api/dev/timeouts/connections/{{timeoutSecondChatConnectionId}}/second-chat-start-now
  body: none
  auth: none
}

script:post-response {
  if (res.getBody().target !== "schedule-negotiation") {
    throw new Error("Expected dev timeout mutation for schedule-negotiation.");
  }
}
