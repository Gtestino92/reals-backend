meta {
  name: 08 Activate Profile A
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/profiles/me/activate
  body: none
  auth: none
}

headers {
  X-Dev-User-Id: {{userAId}}
}

script:post-response {
  const body = res.getBody();
  bru.setEnvVar("profileAStatus", body.status);
}
