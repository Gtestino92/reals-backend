meta {
  name: 30 Get Negotiation
  type: http
  seq: 30
}

get {
  url: {{baseUrl}}/api/connections/{{connectionId}}/negotiation
  body: none
  auth: none
}

script:post-response {
  const body = res.getBody();
  bru.setEnvVar("negotiationId", body.id);
}
