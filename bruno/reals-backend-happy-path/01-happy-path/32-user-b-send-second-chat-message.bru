meta {
  name: 32 User B Send Second Chat Message
  type: http
  seq: 32
}

post {
  url: {{baseUrl}}/api/chat/{{secondChatId}}/messages
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  X-Dev-User-Id: {{userBId}}
}

body:json {
  {
    "content": "Perfecto, seguimos conversando por aca."
  }
}
