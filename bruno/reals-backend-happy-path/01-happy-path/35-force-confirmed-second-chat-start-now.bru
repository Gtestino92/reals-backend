meta {
  name: 35 Force Confirmed Second Chat Start Now
  type: http
  seq: 35
}

post {
  url: {{baseUrl}}/api/local-dev/timeouts/connections/{{connectionId}}/second-chat-available-now
  body: none
  auth: none
}

script:post-response {
  const body = res.getBody();
  if (body.target !== "schedule-negotiation") {
    throw new Error("Expected dev timeout mutation for schedule-negotiation.");
  }
}
