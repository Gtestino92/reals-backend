meta {
  name: 01 Create Counterpart User
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/local-dev/users
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{counterpartEmail}}"
  }
}

script:post-response {
  const body = res.getBody();
  if (!body || !body.id) {
    throw new Error("Expected counterpart user id.");
  }
  bru.setEnvVar("counterpartUserId", body.id);
}
