meta {
  name: 20 Get First Chat
  type: http
  seq: 20
}

get {
  url: {{baseUrl}}/api/matches/{{matchId}}/chat
  body: none
  auth: none
}

headers {
  X-Dev-User-Id: {{userAId}}
}

script:post-response {
  const body = res.getBody();
  bru.setEnvVar("firstChatId", body.id);
}
