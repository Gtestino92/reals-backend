# Data Retention Inventory

> This document describes current application behavior and explicit engineering retention classifications. It is not a legal opinion or a claim of regulatory compliance.

This is the authoritative backend-owned inventory of user-linked and potentially user-identifiable data. It was built from the current JPA entities, Flyway migrations, repositories, storage abstractions, Firebase and push integrations, account deletion/recovery code, audit and safety code, and legal-document actions.

The lifecycle has three distinct stages:

1. **Deletion request:** the user becomes operationally absent immediately. Active interactions are contained and the explicitly classified ephemeral rows are deleted in the account-deletion transaction.
2. **Recovery window:** the local account remains recoverable until `deletionFinalizesAt`. Data needed for recovery and historical product, safety, anti-abuse, legal-evidence, or audit purposes remains. Reactivation does not restore deleted runtime state.
3. **Final deletion / post-recovery retention:** the current implementation only releases or replaces local account identifiers. It is not complete account erasure. Purge or anonymization decisions for retained data remain pending.

The scheduled finalizer runs after the recovery deadline, while
`POST /api/me/deletion/finalization` lets an already deleted user abandon
recovery immediately. Both paths use the same locked finalization mutation:
delete or tolerate absence of the Firebase user, anonymize the local email,
clear `firebaseUid`, clear `deletionFinalizesAt`, keep `status=DELETED`, retain
the historical `authOrigin`, and record `ACCOUNT_DELETION_FINALIZED` once. Soft
deletion alone does not free the email or Firebase UID; permanent finalization
does, so a future signup creates a different backend user that may choose a
different auth origin.

The classifications used below are: `IMMEDIATE_DELETE`, `IMMEDIATE_TERMINATE_RETAIN_HISTORY`, `RETAIN_DURING_RECOVERY`, `RETAIN_ANTI_ABUSE_OR_SAFETY`, `RETAIN_LEGAL_EVIDENCE`, `RETAIN_AUDIT`, `FINAL_PURGE_POLICY_PENDING`, and `EXTERNAL_RETENTION_POLICY_PENDING`.

## Local relational data

| Data category | Physical storage / external system | Primary user linkage | Data or sensitivity description | Product / operational purpose | Source of truth vs derived/recreatable | Deletion-request behavior | Recovery-window behavior | Current finalization behavior | Retention classification | Remaining policy gap |
|---|---|---|---|---|---|---|---|---|---|---|
| Users | PostgreSQL `users` | `id`; email; nullable Firebase UID | Account status, local identifiers, deletion timestamps | Local account identity and lifecycle | Source of truth for local account | Locked, changed from `ACTIVE` to `DELETED`, `deletedAt` and `deletionFinalizesAt` set | Row, email, Firebase UID, and UUID remain reserved for reactivation | Under a row lock, eligibility is revalidated; when a Firebase UID exists, Firebase deletion or confirmed absence must succeed before email replacement and local Firebase UID/deadline release; row and UUID remain | `RETAIN_DURING_RECOVERY` | `FINAL_PURGE_POLICY_PENDING`: broader post-recovery row/data treatment is not defined |
| Profiles | PostgreSQL `profiles` | `user_id` | Display name, birth date, gender, city/countryCode, bio, preferences, profile-authenticity flags | User profile and matchmaking preferences | Source of truth | Status is moved to `DRAFT`; row and fields are not deleted | Retained so the profile can be recovered, but remains `DRAFT` | Unchanged by finalization | `RETAIN_DURING_RECOVERY` | `FINAL_PURGE_POLICY_PENDING`, including profile-authenticity fields |
| Looking-for genders | PostgreSQL `profile_looking_for_genders` | `profile_id` → profile → user | Gender preference set | Match filtering | Source of truth for this preference | Not deleted | Retained with profile | Unchanged by finalization | `RETAIN_DURING_RECOVERY` | `FINAL_PURGE_POLICY_PENDING` |
| Profile-photo metadata | PostgreSQL `profile_photos` | `profile_id` → profile → user | Storage provider/bucket/key, ordering, person/full-body flags, validation and moderation status | Profile media catalog and validation/moderation state | Source of truth for object lookup and photo state | Not deleted | Retained with profile | Unchanged by finalization | `RETAIN_DURING_RECOVERY` | `FINAL_PURGE_POLICY_PENDING`; must be coordinated with object deletion |
| Affinity question answers | PostgreSQL `affinity_question_answers` | `profile_id` → profile → user | Private closed-question answer code plus question id and semantic version | Optional profile-owned compatibility and snapshot input | Source of truth for current user's private affinity answers; catalog questions live only in the versioned resource | Not deleted by account-deletion request; profile is retained and moved to `DRAFT` | Retained with profile; reactivation does not change answers | Unchanged by finalization | `RETAIN_DURING_RECOVERY` | `FINAL_PURGE_POLICY_PENDING`; answers must not be exposed to counterpart-facing endpoints |
| Matchmaking queue | PostgreSQL `matchmaking_queue` | `user_id` | Current participation, precise search latitude/longitude, accuracy, timestamps | Live matchmaking operation | Ephemeral operational state | Bulk-deleted in the deletion transaction | Absent; never restored | Already absent | `IMMEDIATE_DELETE` | None for account deletion; general operational/backup retention remains infrastructure policy |
| Matches | PostgreSQL `matches` | `user_a_id`, `user_b_id` | Pair and lifecycle state/timestamps | Match lifecycle history | Source of truth | Active `CHAT_ACTIVE` becomes `CHAT_REJECTED`; active `VISUAL_PHASE` becomes `VISUAL_REJECTED`; `VISUAL_APPROVED` remains historical | Rows remain; reactivation does not reopen them | Unchanged by finalization | `IMMEDIATE_TERMINATE_RETAIN_HISTORY` | `FINAL_PURGE_POLICY_PENDING` |
| Chats | PostgreSQL `chats` | Through match/connection participants | Chat type, status, lifecycle timestamps and end reason | Chat lifecycle | Source of truth | Visible `AVAILABLE`/`ACTIVE` chats for affected active engagements become `CANCELLED` with `USER_DELETED` | Rows remain and are not reopened | Unchanged by finalization | `IMMEDIATE_TERMINATE_RETAIN_HISTORY` | `FINAL_PURGE_POLICY_PENDING` |
| Chat messages | PostgreSQL `chat_messages` | `chat_session_id` and `sender_id` | User-authored conversation content, timestamps and nullable message reaction metadata (`HEART` or null) | Persisted conversation history and durable message metadata | Source of truth | Not deleted when chat is contained; existing reactions remain attached to retained message rows | Retained | Unchanged by finalization | `RETAIN_DURING_RECOVERY` | `FINAL_PURGE_POLICY_PENDING`, including message, reaction and personal-message retention |
| First-chat guidance | PostgreSQL `first_chat_guidance` | `chat_id` → participants | Active prompt text/ordinal and participant progression timestamps | Guided first-chat state/history | Source of truth for current guidance progression | Not deleted | Retained with chat history | Unchanged by finalization | `RETAIN_DURING_RECOVERY` | `FINAL_PURGE_POLICY_PENDING` |
| Conversation prompt snapshots | PostgreSQL `conversation_prompt_snapshots` | `chat_id` → participants | Immutable first-chat prompt sequence: ordinal, source type/question id, optional affinity semantic version/category/kind, prompt text | Answer-free guided first-chat prompt history | Source of truth for new first-chat sequence advancement | Not deleted | Retained with chat history | Unchanged by finalization | `RETAIN_DURING_RECOVERY` | `FINAL_PURGE_POLICY_PENDING`; contains no answer codes, labels, scores or percentages |
| Chat decisions | PostgreSQL `chat_decisions` | `chat_id`, `match_id` → participants | Per-participant continue/reject decisions | Match progression history | Source of truth | Not deleted | Retained | Unchanged by finalization | `RETAIN_DURING_RECOVERY` | `FINAL_PURGE_POLICY_PENDING` |
| Chat exit requests | PostgreSQL `chat_exit_requests` | requester/responder user IDs and chat | Cancellation/safety request, reason, optional details, resolution | Chat exit workflow/history | Source of truth | Not deleted | Retained | Unchanged by finalization | `RETAIN_DURING_RECOVERY` | `FINAL_PURGE_POLICY_PENDING`, especially free-text details |
| Visual reviews | PostgreSQL `visual_reviews` | `match_id` → participants | Per-participant decisions and optional personal messages | Visual-phase progression and user-authored personal message | Source of truth | Not deleted; active parent match may be terminated | Retained | Unchanged by finalization | `RETAIN_DURING_RECOVERY` | `FINAL_PURGE_POLICY_PENDING`, including personal-message retention |
| Visual-review affinity indicators | PostgreSQL `visual_review_affinity_indicators` | `match_id` → participants | At most three immutable positive shared category id/title snapshots | Answer-free visual-review context | Source of truth for visual-profile affinity indicators | Not deleted; active parent match may be terminated | Retained with match history | Unchanged by finalization | `RETAIN_DURING_RECOVERY` | `FINAL_PURGE_POLICY_PENDING`; contains no question ids, answers, scores, kinds or percentages |
| Connections | PostgreSQL `connections` | `user_a_id`, `user_b_id` | Pair lifecycle and scheduling timestamps | Post-match connection lifecycle | Source of truth | Active connections become `CLOSED` | Rows remain; reactivation does not reopen them | Unchanged by finalization | `IMMEDIATE_TERMINATE_RETAIN_HISTORY` | `FINAL_PURGE_POLICY_PENDING` |
| Schedule negotiations | PostgreSQL `schedule_negotiations` | `connection_id` → participants | Negotiation status, round and confirmed time | Scheduling lifecycle | Source of truth | Pending negotiation for a contained connection becomes `FAILED` | Retained and not reactivated | Unchanged by finalization | `IMMEDIATE_TERMINATE_RETAIN_HISTORY` | `FINAL_PURGE_POLICY_PENDING` |
| Schedule proposals | PostgreSQL `schedule_proposals` | `connection_id` and `user_id` | Proposed meeting date/time, order, round, status | Scheduling proposal history | Source of truth | Not deleted when negotiation is failed | Retained | Unchanged by finalization | `RETAIN_DURING_RECOVERY` | `FINAL_PURGE_POLICY_PENDING` |
| Connection Home dismissals | PostgreSQL `connection_home_dismissals` | `user_id`, `connection_id` | Per-user Home presentation preference | Hide completed/read-only connection cards | Recreatable presentation state | Bulk-deleted in deletion transaction | Absent; never restored | Already absent | `IMMEDIATE_DELETE` | None for account deletion; general backup retention remains infrastructure policy |
| Active engagement locks | PostgreSQL `active_engagement_locks` | `user_id`, engagement ID/type | Current capacity/coordination locks | Enforce active match/connection limits | Ephemeral operational state | Engagement containment releases affected locks; final per-user bulk delete guarantees none remain | Absent; never restored | Already absent | `IMMEDIATE_DELETE` | None for account deletion |
| Penalties | PostgreSQL `penalties` | `user_id`; optional report/admin references | Ban type, reason, active/expiry state | Enforcement and anti-abuse | Source of truth | Not deleted | Retained so deletion/reactivation cannot bypass active penalties; existing expiry lifecycle continues | Unchanged by finalization | `RETAIN_ANTI_ABUSE_OR_SAFETY` | `FINAL_PURGE_POLICY_PENDING`; deletion does not make temporary penalties permanent |
| User reliability events | PostgreSQL `user_reliability_events` | `user_id`; optional lifecycle/report IDs | Short-memory behavior event, score delta, expiry | Reliability scoring and anti-abuse signal | Source events for derived score | Not deleted because of account deletion | Retained until existing expiry cleanup; deletion/reactivation cannot reset still-active state | Unchanged by finalization; existing cleanup still applies | `RETAIN_ANTI_ABUSE_OR_SAFETY` | `FINAL_PURGE_POLICY_PENDING`; existing expiration is unchanged |
| User blocks | PostgreSQL `user_blocks` | blocker and blocked user IDs; optional report | Pair exclusion, source, timestamp | Safety and pair-wide exclusion | Source of truth | Not deleted for either participant | Retained so deletion/reactivation cannot reset pair exclusion | Unchanged by finalization | `RETAIN_ANTI_ABUSE_OR_SAFETY` | `FINAL_PURGE_POLICY_PENDING`; no current unblock operation |
| Safety reports | PostgreSQL `safety_reports` | reporter, reported user, optional admin, lifecycle context IDs | Allegation details, context, review/verdict and penalty references | Safety investigation and enforcement | Source of truth | Not deleted when reporter or reported user deletes | Retained | Unchanged by finalization | `RETAIN_ANTI_ABUSE_OR_SAFETY` | `FINAL_PURGE_POLICY_PENDING`; retention/anonymization period is undecided |
| Safety evidence snapshots | PostgreSQL `safety_report_evidence_snapshots` | report and optional chat/match/connection IDs | Message counts/timestamps and transcript SHA-256, not transcript content | Point-in-time safety evidence metadata | Source of truth for captured evidence summary | Not deleted | Retained | Unchanged by finalization | `RETAIN_ANTI_ABUSE_OR_SAFETY` | `FINAL_PURGE_POLICY_PENDING`; retention/anonymization period is undecided |
| Audit events | PostgreSQL `audit_events` | actor/target user IDs, aggregate ID; request and hashed network/client fields when supplied | Append-style operational event metadata | Auditability of safety, account, profile, legal and lifecycle actions | Source of truth for recorded audit events | Existing rows are not changed; `ACCOUNT_DELETION_REQUESTED` is appended | Retained; IDs are not nulled or rewritten | Successful local finalization appends `ACCOUNT_DELETION_FINALIZED` in the same transaction, without email, Firebase UID or token metadata | `RETAIN_AUDIT` | `FINAL_PURGE_POLICY_PENDING` for retention and anonymization |
| Push device tokens | PostgreSQL `push_device_tokens` | `user_id`; globally unique FCM registration token | Device-routing credential, platform, enabled/last-seen timestamps | Push delivery | Ephemeral operational state | All rows, including disabled rows, are bulk-deleted | Absent; user may register a current token again | Already absent | `IMMEDIATE_DELETE` | None for account deletion; Firebase/FCM provider-side artifacts are external policy |
| Push notification deliveries | PostgreSQL `push_notification_deliveries` | `user_id`, notification type, aggregate ID | Send/deduplication status, provider message ID and error text | Operational push sending and deduplication | Operational record, not lifecycle/audit source of truth | All rows are bulk-deleted | Absent; never restored | Already absent | `IMMEDIATE_DELETE` | None for local account deletion; provider-side retention is external policy |
| Notification preferences | PostgreSQL `user_notification_preferences` | `user_id`, category | Account-level external push group toggles for `ACTIVITY`, `REMINDERS` and `AVAILABILITY`; missing rows mean enabled | Backend-authoritative push suppression preferences | Source of truth for explicit account preference overrides; defaults are derived | All rows are bulk-deleted | Absent; defaults apply if the user reactivates without setting preferences again | Already absent | `IMMEDIATE_DELETE` | None for local account deletion |
| User Home status | PostgreSQL `user_home_status` | primary key `user_id` | Projection version, dirty flag, update time | Polling/invalidation projection | Derived and recreatable | Deleted after all required account/engagement Home invalidations | Absent; reactivation does not restore it; `HomeStatusService.getOrCreateStatus` creates a fresh row when needed | Already absent unless later recreated by active use | `IMMEDIATE_DELETE` | None for account deletion |
| User legal-document actions | PostgreSQL `user_legal_document_actions` | `user_id` | Document type/version, nullable document content SHA-256, action, timestamp | Record application-level acceptance/acknowledgement action and backend historical content identity when available | Source of truth for the recorded action and, for new BACK-7 rows, the canonical content SHA-256; pre-BACK-7 rows may be unanchored with null hash | Not deleted | Retained; legal satisfiability requires current type, version, action and content SHA-256 | Unchanged by finalization | `RETAIN_LEGAL_EVIDENCE` | `FINAL_PURGE_POLICY_PENDING`; production legal publication and retention/anonymization policy remain separate decisions |

## External and operational artifacts

| Data category | Physical storage / external system | Primary user linkage | Data or sensitivity description | Product / operational purpose | Source of truth vs derived/recreatable | Deletion-request behavior | Recovery-window behavior | Current finalization behavior | Retention classification | Remaining policy gap |
|---|---|---|---|---|---|---|---|---|---|---|
| Profile-photo objects | S3-compatible storage: local/hosted MinIO, Cloudflare R2, or configured provider | Object key under `users/<userId>/profile-photos/...`; metadata row | Server-normalized JPEG profile-photo bytes | Render profile photos | Object is normalized display media; read URL is derived/presigned | No object-storage deletion is invoked | Objects remain available through retained metadata | Unchanged by finalization | `RETAIN_DURING_RECOVERY` | `FINAL_PURGE_POLICY_PENDING`: post-recovery object deletion and backup/version handling |
| Firebase Auth account and local mapping | Firebase Auth plus `users.firebase_uid`/email | Firebase UID and email | External identity account and identifiers | Authentication | Firebase Auth is external identity source; local mapping controls application account | Firebase account and local UID remain; after commit the backend requests refresh-token revocation. The external user is not deleted during the recovery window | Firebase account and local UID remain for ownership authentication and reactivation; reactivation may set the external account `disabled=false` | Reals synchronously initiates Firebase Auth user deletion before releasing the local UID. `DELETED` and Firebase `USER_NOT_FOUND` (`ALREADY_ABSENT`) permit local finalization; other failures leave identifiers and deadline pending for retry | `EXTERNAL_RETENTION_POLICY_PENDING` | Provider-managed residual copies, backups and physical-erasure timing remain governed by Firebase policy; Reals does not claim immediate physical erasure of every provider copy |
| Firebase refresh-token revocation state | Firebase Auth | Firebase UID and provider-managed token validity timestamps | Provider-managed session invalidation state | End existing Firebase sessions after deletion commit | External provider state; no local row | Revocation is requested after successful commit; failure is logged and is not a rollback of the committed local deletion | No local snapshot is restored; account may be enabled on reactivation | No additional action | `EXTERNAL_RETENTION_POLICY_PENDING` | Provider retention and retry/reconciliation policy are unresolved |
| FCM provider delivery artifacts | Firebase Cloud Messaging | Device token/provider message ID | Provider-side send and delivery metadata | Push transport | External provider state | Local token/delivery rows are deleted; no provider-side deletion API is invoked | Not restored locally | No additional action | `EXTERNAL_RETENTION_POLICY_PENDING` | Provider-side retention is not controlled by this repository |
| Application caches | In-process Caffeine profile, active-lock and active-penalty caches | User ID cache keys/values | Derived profile and enforcement state | Performance | Derived/recreatable; bounded by configured expiry | No account-deletion-specific cache purge was found | Entries expire through existing cache policy/process lifetime; authoritative database state controls behavior | No finalization-specific action | `EXTERNAL_RETENTION_POLICY_PENDING` | Explicit deletion-time eviction and runtime retention policy require separate review |
| Rate-limit buckets | In-process Caffeine cache | Hash of bearer token or client IP plus route rule | Request-volume state with hashed client identity | Abuse/rate limiting | Ephemeral/recreatable | No targeted deletion; expires after inactivity or process restart | May remain until existing expiry | No finalization-specific action | `EXTERNAL_RETENTION_POLICY_PENDING` | Infrastructure/runtime retention and deletion linkage are not defined |
| Application/runtime logs | Runtime stdout/log aggregation platform | May contain request ID, user/aggregate IDs and operational errors; account deletion can log Firebase UID on failure | Potentially identifiable operational telemetry; request/response bodies are not intentionally logged by the inspected code | Diagnostics and operations | Operational artifact, not product source of truth | No application-controlled deletion | Governed by deployment/log platform, not account recovery code | No finalization-specific action | `EXTERNAL_RETENTION_POLICY_PENDING` | Log retention, access, redaction and deletion policy are infrastructure work |
| Metrics and health telemetry | Spring Actuator/runtime monitoring platform | Current configuration disables application metrics globally; future labels or infrastructure metrics could still be identifying | Aggregated operational measurements | Monitoring | Derived operational artifact | No account-deletion action | Platform-dependent | No finalization-specific action | `EXTERNAL_RETENTION_POLICY_PENDING` | Metric label and platform retention policy require infrastructure review |
| Database/object-store backups | Database and storage-provider backup/snapshot systems | Copies of any user-linked rows or photo objects | Historical copies of persisted data | Disaster recovery | Infrastructure copies | Not modified by online account-deletion transaction | Platform-dependent | Online finalization does not rewrite existing backups | `EXTERNAL_RETENTION_POLICY_PENDING` | Backup schedules, expiry, restore-time deletion replay, and access controls are unresolved |

`shedlock` contains scheduler coordination timestamps and lock owner names, not user-linked product data. No profile-authenticity provider artifact, live reference image, face embedding or biometric template exists in the current implementation because the configured provider implementation is local/no-op; a future external provider requires an additional inventory row and biometric/privacy/retention policy.

## Implemented deletion boundary

`AccountDeletionImmediateCleanupService` owns only bulk deletion of:

- `matchmaking_queue`
- `active_engagement_locks`
- `push_device_tokens`
- `push_notification_deliveries`
- `user_notification_preferences`
- `connection_home_dismissals`
- `user_home_status`

`AccountDeletionService` separately owns active Match, Chat, Connection, and ScheduleNegotiation containment and retains their history. Cleanup runs after deleted-user and counterpart Home invalidations, so counterpart projection bumps remain while the deleted user's projection is absent at commit. Any cleanup failure propagates and rolls back the account-deletion transaction.

## Explicit remaining debt

No retention duration is selected here. Separate product, privacy, legal, security, and infrastructure decisions are still required for:

- post-recovery profile retention/purge
- profile-photo metadata and object deletion, including stored-object backups or versions
- private affinity-question and profile-question answer retention or purge after final deletion
- chat/message and visual-review personal-message retention
- lifecycle-row retention or anonymization
- block retention after final deletion
- penalty and reliability-event retention after final deletion without overriding existing expiry behavior
- safety-report and evidence retention/anonymization periods
- audit retention/anonymization
- legal-action retention and production legal-document publication evidence
- Firebase Auth external-user deletion/finalization and provider-managed token state
- FCM provider artifacts
- database and object-storage backup retention and restore handling
- application log, metric, cache, and rate-limit state retention

Until those decisions are implemented, finalization after `deletionFinalizesAt` must be described as **coordinated Firebase Auth deletion and local identifier finalization/release**, not complete product-data purge or immediate physical erasure of all provider-managed copies.
