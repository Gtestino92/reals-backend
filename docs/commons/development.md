# Development Conventions

## Branch Naming

Work branches created or renamed for backend changes must start with `feature/` unless the task explicitly requires another branch type.

Use a short kebab-case slug after the prefix, for example:

```text
feature/safety-admin-report-hardening
```
